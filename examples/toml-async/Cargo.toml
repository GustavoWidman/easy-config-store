[package]
name = "toml-async"
version = "0.1.0"
edition = "2024"

[dependencies]
eyre = "0.6.12"
easy-config-store = { path = "../..", features = ["tokio"] }
tokio = { version = "1.44.1", features = ["full"] }
serde = { version = "1.0.219", features = ["derive"] }
