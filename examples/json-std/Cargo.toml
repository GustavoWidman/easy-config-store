[package]
name = "json-std"
version = "0.1.0"
edition = "2024"

[dependencies]
eyre = "0.6.12"
easy-config-store = { path = "../..", default-features = false, features = [
  "json",
] }
serde = { version = "1.0.219", features = ["derive"] }
